<div class="container">
  <h2 class="text-center">The Team Behind Ember</h2>
  <section class="margin-bottom-medium" aria-labelledby="team-detail">
    <p class="text-center">
      Ember is an Open Source project that relies on the tireless support of individual contributors. These are the teams
      that guide the development and instruction of Ember.js.
    </p>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="team">
    <h3 class="text-center">The Steering Committee</h3>
    <p class="text-center">
      The Steering Committee is responsible for the overall governance of the Ember project.
    </p>
    <div class="layout-grid">
      {{#each steeringCommitteeMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="team-core">
    <h3 class="text-center">The Ember.js Core Team</h3>
    <div class="layout-grid">
      {{#each coreTeamMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="team-core">
    <h3 class="text-center">The Ember.js Core Team</h3>
    <div class="layout-grid">
      {{#each coreTeamMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="team-cli-core">
    <h3 class="text-center">The Ember CLI Core Team</h3>
    <p class="text-center">
      The Ember CLI core team is responsible for maintaining ember-cli, the command line interface for managing and packaging
      Ember.js applications and addons. The team also maintains many of the addons in the default blueprint as well as
      fastboot.
    </p>
    <div class="layout-grid">
      {{#each coreCLITeamMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="team-data-core">
    <h3 class="text-center">The Ember Data Core Team</h3>
    <p class="text-center">
      The Ember Data core team is responsible for the official data persistence library for Ember.js applications.
    </p>
    <div class="layout-grid">
      {{#each dataTeamMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="team-learning-core">
    <h3 class="text-center">The Ember Learning Core Team</h3>
    <p class="text-center">
      The mission of the Ember Learning core team is to empower Ember users to learn, build and teach. This team is responsible
      for keeping the guides and API documentation updated, and manages the initiatives that support learning Ember.
    </p>
    <div class="layout-grid">
      {{#each learningTeamMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
  <section class="margin-bottom-medium" aria-labelledby="alumni">
    <h3 class="text-center">Emeritus</h3>
    <p class="text-center">
      Serving as a member of the Core Team(s) of an open source project like Ember is a huge amount of work. These are
      the emeritus members of the Ember Core Teams, to whom we will always be grateful.
    </p>
    <div class="layout-grid">
      {{#each alumniTeamMembers as |member|}}
      {{team-list-item
      image=member.image
      name=member.name
      twitter=member.twitter
      github=member.github
      }}
      {{/each}}
    </div>
  </section>
</div>
<!--container-->